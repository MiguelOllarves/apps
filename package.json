{
  "name": "control-total",
  "version": "1.0.0",
  "description": "Root project for ControlTotal",
  "scripts": {
    "backend:dev": "npm run start:dev --prefix backend",
    "frontend:dev": "npm run dev --prefix frontend",
    "start:all": "concurrently \"npm run backend:dev\" \"npm run frontend:dev\""
  },
  "devDependencies": {
    
    "concurrently": "^9.2.1"
  }
}
